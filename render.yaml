services:
  - type: web
    name: helpdesk-web
    env: python
    plan: standard
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn helpdesk_system.wsgi:application --log-file -
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: helpdesk_system.settings.prod
      - key: DATABASE_URL
        fromDatabase:
          name: helpdesk-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: yourrenderapp.onrender.com
      - key: DJANGO_ENV
        value: prod
      - key: DEBUG
        value: "False"

  - type: worker
    name: helpdesk-celery
    env: python
    plan: standard
    buildCommand: pip install -r requirements.txt
    startCommand: celery -A helpdesk_system worker --loglevel=info
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: helpdesk_system.settings.prod
      - key: DATABASE_URL
        fromDatabase:
          name: helpdesk-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: SECRET_KEY
        value: ${SECRET_KEY}
      - key: DJANGO_ENV
        value: prod

  - type: worker
    name: helpdesk-beat
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: celery -A helpdesk_system beat --loglevel=info
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: helpdesk_system.settings.prod
      - key: DATABASE_URL
        fromDatabase:
          name: helpdesk-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          name: helpdesk-redis
          type: redis
          property: connectionString
      - key: SECRET_KEY
        value: ${SECRET_KEY}
      - key: DJANGO_ENV
        value: prod

  - type: redis
    name: helpdesk-redis
    plan: starter
    ipAllowList: []

databases:
  - name: helpdesk-postgres
    plan: starter
    databaseName: helpdesk
    user: helpdesk_user